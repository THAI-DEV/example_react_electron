{
  "name": "react-electron",
  "description": "test",
  "author": "DECH",
  "version": "0.1.0",
  "private": true,
  "main": "public/electron.js",
  "homepage": "./",
  "dependencies": {
    "electron-is-dev": "^1.0.1",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "update-electron-app": "^1.3.0",
    "cross-env": "5.2.0",
    "electron-log": "^3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron-build": "electron-builder",
    "release": "yarn build && electron-builder --publish=always",
    "build-app": "yarn build && yarn electron-build",
    "start-app": "concurrently \"cross-env BROWSER=none yarn start\" \"wait-on http://localhost:3000 && electron .\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "react-scripts": "2.1.5",
    "concurrently": "^4.1.0",
    "electron": "^4.0.5",
    "electron-builder": "^20.38.5",
    "wait-on": "^3.2.0"
  },
  "build": {}
}
